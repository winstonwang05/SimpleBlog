spring:
  application:
    name: user-center
#  mysql Connection
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/usercenter
    username: root
    password: 123456
  session:
    timeout: 86400

# port setting
server:
  port: 8080
# servlet:
#   context-path: /api
# close camel
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    # 逻辑删除
  global-config:
      db-config:
        logic-delete-field: isDelete # 全局逻辑删除字段名(deleted为实体类属性名称)
        logic-delete-value: 1 # 逻辑已删除值。可选，默认值为 1
        logic-not-delete-value: 0 # 逻辑未删除值。可选，默认值为 0
# 配置全局密钥pepper
user:
  password:
    pepper: "xk8#jF$5mL@9qR!2tZvP*7sW&3yB^6nCdK%4hGg" # 40+字符的随机字符串